# Install backend/requirements.txt first. The CustomTkinter desktop UI has
# intentionally been removed; Pillow remains part of the artwork pipeline.
Pillow>=10.4.0,<12.0

# ─── Downloading & Media Tooling ─────────────────────────────────────
yt-dlp>=2025.9.5
mutagen>=1.47.0
imageio-ffmpeg>=0.5.1          # Auto-provisions ffmpeg binary; no PATH required

# ─── Discovery APIs ──────────────────────────────────────────────────
# Discovery talks to Discogs over raw HTTP via `requests`; the official
# discogs client is intentionally not used.
ytmusicapi>=1.8.2
requests>=2.32.0

# ─── DSP / Analysis ──────────────────────────────────────────────────
librosa>=0.10.2,<0.12
numpy>=1.26,<3.0
scipy>=1.13
soundfile>=0.12.1
audioread>=3.0.1

# ─── Audio Preview Playback ──────────────────────────────────────────
# In-app waveform preview player. sounddevice bundles PortAudio wheels
# on Windows/macOS; plays decoded PCM straight from a numpy buffer with
# frame-accurate seeking for the discovery + vault preview scrubbers.
sounddevice>=0.4.6

# ─── Stem Separation ─────────────────────────────────────────────────
# Pinned exact stack for reproducible Python 3.14 runtime behavior.
# Keep torch / torchaudio / torchcodec aligned.
torch==2.11.0
torchaudio==2.11.0
# torchcodec intentionally excluded — the Demucs worker patches Torchaudio
# loading through bundled FFmpeg and saving through SoundFile.
demucs==4.0.1
# Optional: needed only for mdx_extra_q model quality preset.
# If unavailable, the app now falls back to mdx_extra at runtime.
# diffq>=0.2

# ─── Persistence & Config ────────────────────────────────────────────
# sqlite3 is stdlib; no dependency needed
pydantic>=2.8                  # Used by utils/config.py for schema validation
keyring>=25.0                  # OS credential store for Discogs/DeepSeek keys

# ─── Packaging (dev only, not required at runtime) ───────────────────
pyinstaller>=6.10 ; extra == "build"
